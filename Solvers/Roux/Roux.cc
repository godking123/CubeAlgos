#include "Roux.h"

namespace Roux {

// Placeholder, Roux stages (first block, second block, CMLL, LSE) each need a
// recogniser and algorithm set, none written yet
// Main gates on Method::implemented, so returning nothing is never read as a solve

void buildTables() {
}

std::vector<Move> solve(const CubeState& s) {
    (void)s;
    return {};
}
} // namespace Roux
